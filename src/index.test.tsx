import { Grid, Button, Col } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(Grid).toBeTruthy()
    expect(Button).toBeTruthy()
    expect(Col).toBeTruthy()
  })
})
